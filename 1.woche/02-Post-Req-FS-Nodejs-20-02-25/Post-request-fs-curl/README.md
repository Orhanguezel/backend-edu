# Backend : fs nodejs, Post Request, curl, Get Request, live demo React with fs nodejs.

## Branches :

- curl : Wie funktioniert curl und ein Request damit machen.

- fs : File system node js

- Post-request : Post Request with curl
